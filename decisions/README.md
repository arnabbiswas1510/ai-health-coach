# Architectural Decision Records (ADRs)

This directory contains key Architectural Decision Records (ADRs) for `ai-health-coach`.

Every meaningful architectural change, logic redesign, or schema migration should be recorded as a Markdown document in this directory using the format `YYYY-MM-DD_<short-description>.md`.
